id: flatpak.test.app
runtime: org.freedesktop.Platform
runtime-version: &runtime-version '24.08'
sdk: org.freedesktop.Sdk
finish-args:
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --device=all
  - --socket=pulseaudio
  - --share=network
  - --allow=multiarch
command: Test
add-extensions:
  flatpak.test.app.extension:
    directory: extensions
    add-ld-path: lib
    merge-dirs: plug-ins;help
    subdirectories: true
    no-autodownload: true
    autodelete: true
cleanup-commands:
modules:
  - name: Test
    buildsystem: simple
    sources:
      - type: file
        path: flatpak.test.app.appdata.xml
      - type: file
        path: Scripts/Test
    build-commands:
      - |
        # Install flatpak.test.app.appdata.xml in /app/share/appdata
        install -Dm644 flatpak.test.app.appdata.xml -t /app/share/appdata

        # Install Test in /app/bin
        install -Dm744 Test -t /app/bin

        # Create /app/extensions directory
        mkdir -p /app/extensions
